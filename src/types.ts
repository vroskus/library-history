import type {
  Location,
} from 'history';
import type {
  match,
  RouteComponentProps,
} from 'react-router';

export type $Location = Location;
export type $Match = match;
export type $WebRouter = RouteComponentProps;
